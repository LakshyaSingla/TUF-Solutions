            }else{
                temp.add(nums[i]);
                i++;
        List<Integer> temp = new ArrayList<>();
        while(i <= mid && j <= high){
            if(nums[i] <= nums[j]){
        long count = 0;
    long merge(int[] nums, int low ,int mid, int high){
        int i = low;
        int j = mid + 1;
class Solution {
